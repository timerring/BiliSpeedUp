{
  "name": "BiliSpeedUp",
  "version": "1.0.0",
  "description": "B站倍速播放增强脚本 - 支持自定义倍速、鼠标滚轮调节、触控板调节、记忆倍速",
  "type": "module",
  "main": "dist/BiliSpeedUp.js",
  "scripts": {
    "build": "rollup -c",
    "build:watch": "rollup -c -w",
    "dev": "rollup -c -w",
    "build:userscript": "npm run build && node scripts/build-userscript.js",
    "build:extension": "npm run build && node scripts/build-extension.js",
    "build:all": "npm run build && node scripts/build-userscript.js && node scripts/build-extension.js"
  },
  "keywords": [
    "bilibili",
    "video",
    "speed",
    "playback",
    "userscript"
  ],
  "author": "timerring",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^4.9.0"
  }
}
